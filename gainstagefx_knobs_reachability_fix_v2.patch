diff --git a/tests/knobs.rs b/tests/knobs.rs
--- a/tests/knobs.rs
+++ b/tests/knobs.rs
@@ -308,6 +308,12 @@ fn every_control_is_either_reachable_or_given_a_resting_position() {
         if let Some((b, m, t)) = gain.own_tone() {
             reachable.extend([b, m, t]);
         }
+        if let Some((which, _)) = gain.own_sweep() {
+            reachable.push(which);
+        }
+        if let Some(((lo, _), (hi, _))) = gain.own_colour_mix() {
+            reachable.extend([lo, hi]);
+        }
         if gain.has_reverb_and_tremolo() {
             reachable.extend([twin::REVERB, twin::INTENSITY]);
         }
